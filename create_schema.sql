CREATE TABLE `indoor_navigation_logs`.`indoor_logs` (
  `id` INT NOT NULL,
  `scenario_num` VARCHAR(45) NULL,
  `point_num` VARCHAR(45) NULL,
  `device_name` VARCHAR(45) NULL,
  `rssi` VARCHAR(45) NULL,
  `tx_power` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));

